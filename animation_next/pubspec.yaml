name: animation_next
description: A comprehensive Flutter animation package providing smooth, customizable animations including bounce, fade, slide, zoom, flip, and color tween animations.
version: 0.1.0
homepage: https://github.com/shashiben/flutter_next
repository: https://github.com/shashiben/flutter_next
issue_tracker: https://github.com/shashiben/flutter_next/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
