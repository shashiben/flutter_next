/// Enum for grid prefixes.
enum GridPrefix {
  xxl,
  xl,
  lg,
  md,
  sm,
  xs,
}

/// Enum for Next UI variants.
enum NextVariant {
  primary,
  secondary,
  success,
  danger,
  warning,
  info,
  light,
  dark,
  custom,
}

/// Enum for Next button variants.
enum NextButtonVariant {
  filled,
  outlined,
}

/// Enum for Next fade-in animation variants.
enum NextFadeInVariant {
  fadeInLeft,
  fadeInRight,
  fadeInTop,
  fadeInBottom,
}

/// Enum for Next fade-out animation variants.
enum NextFadeOutVariant {
  fadeOutLeft,
  fadeOutRight,
  fadeOutTop,
  fadeOutBottom,
}

/// Enum for Next slide animation variants.
enum NextSlideVariant {
  slideInTop,
  slideInBottom,
  slideInLeft,
  slideInRight,
}

/// Enum for Next zoom animation variants.
enum NextZoomVariant {
  zoomIn,
  zoomOut,
}

/// Enum for Next bounce animation variants.
enum NextBounceVariant {
  bounceInTop,
  bounceInBottom,
  bounceInLeft,
  bounceInRight,
}

/// Enum for Next flip animation variants.
enum NextFlipVariant {
  flipX,
  flipY,
}

/// Enum for Next breadcrumb variants.
enum NextBreadcrumbVariant {
  wrap,
  scroll,
}
