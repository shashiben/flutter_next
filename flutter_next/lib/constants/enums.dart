part of '../flutter_next.dart';

/// Enum for grid prefixes.
enum GridPrefix { xxl, xl, lg, md, sm, xs }

/// Enum for Next UI variants.
enum NextVariant {
  primary,
  secondary,
  success,
  danger,
  warning,
  info,
  light,
  dark,
  custom,
}

/// Enum for Next button variants.
enum NextButtonVariant { filled, outlined }

/// Enum for Next breadcrumb variants.
enum NextBreadcrumbVariant { wrap, scroll }
